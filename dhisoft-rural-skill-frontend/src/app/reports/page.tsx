'use client';import {useEffect,useState}from'react';import{api}from'@/lib/api';export default function Reports(){const[data,setData]=useState<Record<string,unknown>|null>(null);const[error,setError]=useState('');useEffect(()=>{api<Record<string,unknown>>('/workflow/report').then(setData).catch(e=>setError(e instanceof Error?e.message:'Sign in to view outcomes.'))},[]);return <><div className="breadcrumb">Overview <span>/</span> Reports</div><div className="page-heading"><div><p className="eyebrow">TENANT OUTCOMES</p><h1>Outcome dashboard</h1><p className="lede">Connect verified capability to safe work, retention and income without exposing another tenant’s data.</p></div></div>{error&&<div className="alert error">{error}</div>}<div className="dashboard-grid">{Object.entries(data||{}).map(([key,value])=><div className="metric card" key={key}><span className="metric-label">{key}</span><strong className="metric-value">{String(value)}</strong></div>)}</div></>}
